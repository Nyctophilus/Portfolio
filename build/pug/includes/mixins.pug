mixin smileys(n)
	- for (let i = 0; i < n; i++){
		<label class="smile">
			<input type="radio"/>
			<div class="face"></div>
		</label>
	- }

mixin spanify(word)
	-const arrWord = word.split("");
		for w in arrWord
			<span>#{w}</span>